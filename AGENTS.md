# XZ Repository Agent Instructions

你正在为 XZ（鲜知）开发“硬件就绪、健康扩展就绪”的 AI 数字冰箱系统。当前是纯软件 MVP，未来会接入 AI 玩偶冰箱贴、营养计算、个人摄入、健康指标和家庭饮食 AI Agent。

## 1. 执行优先级

1. 正确维护库存事实。
2. 保证用户确认和可撤销。
3. 保证家庭数据隔离与隐私。
4. 保持代码简单、显式、可测试。
5. 为未来硬件和健康模块保留稳定接口，但不提前实现未验证功能。

## 2. 固定架构规则

- 架构模式：模块化单体。
- 当前入口：PWA 语音、PWA 手动操作。
- 未来入口：AI 冰箱贴，通过 Adapter 接入。
- 所有写操作必须转成 Command，由 Application Service 执行。
- Controller、UI、LLM、ASR Provider 不得直接写领域表。
- 模块只能写自己拥有的表；跨模块通过公开 Application API 或 Domain Event 协作。
- LLM 只能输出结构化候选、解释或建议，不得直接修改库存、健康指标或个人摄入。
- 查询可以自动执行；库存写操作默认需要用户确认。
- 所有写操作必须支持 idempotency key。
- 库存使用、食物丢弃、个人摄入、菜品制作是不同业务事实，不得混用。

## 3. 核心模块

- `household`：家庭、成员、角色与授权。
- `food-knowledge`：标准食材、别名、单位、保质期、营养引用。
- `inventory`：数字冰箱、存储区域、库存批次与交易流水。
- `interaction`：语音任务、手动输入、ASR、意图解析、确认状态。
- `realtime-notification`：实时同步、提醒和消息投递。
- `device`：未来设备身份、设备状态与命令契约；MVP 只预留。
- `meal-intake`：未来菜品、份量和个人摄入；MVP 只定义契约。
- `health`：未来健康档案、指标和目标；MVP 只定义契约。
- `ai-agent`：受控工具编排、洞察、报告和建议；不得绕过领域服务。

## 4. 编码规则

- TypeScript 使用 strict mode；禁止无理由的 `any`、非空断言和类型逃逸。
- 业务规则放在 domain/application 层，不放在 React 组件、Controller、ORM Hook 或 Prompt 中。
- 优先写清晰的显式代码，不为复用而创建抽象。
- 外部服务必须通过 Adapter；禁止在领域代码中直接调用供应商 SDK。
- 错误必须分类并可观察；禁止空 `catch`、静默失败和仅打印日志后继续。
- 金额、重量、数量和营养值必须带单位；禁止裸浮点表达业务量。
- 时间统一保存 UTC，展示时按家庭时区转换。
- 数据库 migration 不得手工修改生产库。
- 结构化日志不得包含原始音频、未脱敏健康数据或完整用户语音文本。

## 5. 每次任务工作流

1. 读取相关文档和代码。
2. 说明当前行为、目标行为、影响模块和风险。
3. 给出最小变更计划；未获授权不要扩大范围。
4. 先补测试或定义验收测试。
5. 实现最小可用变更。
6. 运行 lint、typecheck、unit、integration、build。
7. 输出：改动文件、测试结果、未解决问题、是否需要 ADR 或 migration。

## 6. 停止并请求确认的情形

- 需要改变模块边界或数据所有权。
- 需要引入新基础设施、中间件或付费供应商。
- 需要删除或批量重写现有数据。
- 需求会把库存消耗错误地当成个人摄入。
- 健康建议可能被理解为诊断或治疗。
- 无法通过现有事实判断正确行为。

## 7. Definition of Done

- 验收标准全部满足。
- 无越权写表、无重复扣减、无负库存。
- 新行为有测试；失败路径有测试。
- API、事件和 migration 有文档。
- lint、typecheck、test、build 全部通过。
- 没有把临时代码、密钥、原始音频或敏感数据提交到仓库。

# Refactor Prompt

请评估并重构：

`{{REFACTOR_TARGET}}`

先回答：当前具体痛点是什么，是否有测量证据，为什么不保持现状。

规则：

- 行为保持不变，先锁定测试；
- 一次只解决一个结构问题；
- 不为假设中的复用创建框架；
- 不同时升级依赖、改 Schema 和开发功能；
- 保持模块数据所有权；
- 公共 API 或事件变化必须版本化；
- 给出回滚路径。

输出：现状、目标、替代方案、trade-off、分步计划、代码、测试结果、剩余风险。改变架构边界时先写 ADR。
